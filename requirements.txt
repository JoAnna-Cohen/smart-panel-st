# SmartThings connector (app.py / worker.py). The Home Assistant integration
# in custom_components/ declares its own requirements in manifest.json.
flask>=3.0.0
python-dotenv>=1.0.0
requests>=2.31.0
aiohttp>=3.9.0
cryptography>=41.0.0
gunicorn>=21.2.0
